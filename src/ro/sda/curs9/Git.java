package ro.sda.curs9;

public class Git {
    //pull, rebase, branch, branch-b, push, merge
    // rebase - ma ajuta sa im actualizezi branchul meu cu ultimele modoficari de pe master tulpina -master branchul principal
    //merge din versiuni facem o singura versiune
    // push incarca commiturile pe server
    //branch - cream un branch nou si sa trecem la o alta ramura
    // git stash un fel de commit ar se salveaza local intr un cache -stash apply incarca
}
